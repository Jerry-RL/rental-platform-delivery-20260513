# 租车平台第三方接口与成本控制说明

版本：v1.1  
日期：2026-06-01

## 1. 对接范围

本阶段涉及两类第三方接口：

- **违章查询**：[数脉科技 — 全国车辆违章详情查询 API](https://www.shumaiapi.com/productDetail/25)（产品 ID 25）
- GPS定位：已联系厂商平台 API

> 对接细则见 [integration-shumai-violation-v1.md](./03-API/规范/integration-shumai-violation-v1.md)

## 2. 供应商接口清单

| 类别 | 供应商 | 核心用途 | 计费 | 调用方式 |
|---|---|---|---|---|
| 违章 | **数脉科技（杭州数脉）** | 全国未处理违章详情（车牌+发动机号+车架号+车型） | **¥0.06/次**；套餐 **¥600/1万次** | HTTPS REST |
| GPS | 厂商平台 | 实时定位、轨迹、在线状态 | 按厂商合同 | HTTPS REST |
| 地图 | 待确认 | 位置输入、坐标解析、轨迹展示 | 按厂商标准 | SDK/API |

### 2.1 数脉违章 API 要点

| 项 | 说明 |
|---|---|
| 官方页面 | https://www.shumaiapi.com/productDetail/25 |
| 平台编码 | `SHUMAI` |
| 单价 | ¥0.06/次（与「600元/1万次」套餐折合单价一致） |
| 入参 | 车牌号、发动机号、车架号（VIN）、车辆类型 |
| 审核 | 企业用户需提交 **应用场景审核**（租车/汽车租赁） |
| 体验 | 新用户 0 元 / 3 次；套餐有效期二年（以购买页为准） |

## 3. 接口接入设计

### 3.1 统一适配层

- 新增 `Integration Adapter` 层，隔离供应商协议差异
- 内部统一模型：`provider`、`request_id`、`status`、`cost`、`raw_payload`
- 避免业务服务直接依赖供应商字段

### 3.2 接口治理

- 重试：网络错误与5xx支持指数退避重试
- 熔断：连续失败触发熔断，快速失败保护核心链路
- 限流：按租户与接口维度限流
- 告警：错误率、超时率、成本异常均触发告警

## 4. 成本模型与配额策略

### 4.1 成本模型

- 违章查询按 **数脉按次计费**，默认单价 **¥0.06 / 车 / 次**（`integration_cost_ledger.unit_cost`）
- 批量任务：`预估成本 = 车辆数 × 0.06`；`实际成本 = 成功查询车辆数 × unit_cost`
- 推荐套餐：**¥600 购买 1 万次**（适合 200 台车队中高频巡检或多年摊销）
- GPS费用按厂商套餐和调用量执行（以商务合同为准）

**200 台车队参考（默认每月 2 次批量巡检，每次查满 200 台）**

```
年调用次数 = 200 台 × 2 次/月 × 12 月 = 4,800 次
年违章 API 成本 ≈ 4,800 × 0.06 = ¥288
```

若改为每月全量查 1 次：`200 × 12 × 0.06 = ¥144/年`。

### 4.2 配额策略（建议默认）

- 默认每月免费查询 2 次（按租户）
- 超额策略可配置：
  - `DENY`：直接拒绝
  - `APPROVAL`：审批后执行
  - `PAID`：继续执行并记账

## 5. 数据落库建议

### 5.1 关键数据表

- `violation_query_quota`：月度配额、已用次数、策略
- `violation_query_task`：批量任务主表
- `violation_query_item`：车辆级结果明细
- `integration_call_log`：第三方调用日志
- `integration_cost_ledger`：第三方成本台账

### 5.2 核心字段

- 幂等：`idempotency_key`
- 审计：`operator_id`、`request_id`、`trace_id`
- 成本：`unit_cost`、`quantity`、`total_cost`
- 错误：`error_code`、`error_message`

## 6. 接口流程伪代码

```pseudo
FUNCTION callProviderWithGovernance(provider, endpoint, payload):
  key = makeIdempotencyKey(provider, endpoint, payload)
  IF existsSuccessLog(key):
    RETURN cachedResult(key)

  TRY:
    response = httpCall(provider, endpoint, payload, timeout=3s)
    normalized = normalize(provider, endpoint, response)
    writeCallLog(key, provider, endpoint, "SUCCESS", normalized.cost)
    RETURN normalized
  CATCH timeoutOr5xx:
    retryResult = retryWithBackoff(max=3)
    IF retryResult.success:
      writeCallLog(key, provider, endpoint, "SUCCESS_RETRY", retryResult.cost)
      RETURN retryResult

    openCircuitIfNeeded(provider, endpoint)
    writeCallLog(key, provider, endpoint, "FAILED", 0)
    RETURN error("PROVIDER_UNAVAILABLE")
```

## 7. 地图授权与合规建议

- 先完成法务与商务确认，再启用高风险地图能力
- 未确认授权阶段仅启用必要能力（地理编码/定位输入）
- 轨迹展示优先走 GPS 厂商提供能力，减少独立地图授权压力
- 合规留痕：保留“能力启停记录、授权证明、策略变更日志”

## 8. 上线前检查清单

- [ ] 供应商账号、密钥、白名单配置完成
- [ ] 超时/重试/熔断参数完成压测
- [ ] 配额与成本统计与财务口径一致
- [ ] 异常告警（失败率/超时/成本突增）联调通过
- [ ] 地图授权状态已确认并形成文档留档
